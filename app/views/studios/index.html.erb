<h1> Studios </h1>
	<% @studios.each do |studio| %>
		<h3><%= studio.name %></h3>
		<% studio.movies.each do |movie| %>
			<section id = "movie-<%= movie.id %>">
				<p><%= movie.name %>
			</section>
		<% end %>	
	<% end %>
	
